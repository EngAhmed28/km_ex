# هيكل المشروع - King of Muscles

## نظرة عامة

المشروع مقسم إلى 3 مجلدات رئيسية:

```
km_ex/
├── front/          # تطبيق React Frontend
├── back/           # API Backend (Node.js + Express + MySQL)
└── docs/           # ملفات التوثيق والـ AI
```

## مجلد الباك إند (back/)

```
back/
├── config/
│   ├── database.js      # إعدادات اتصال MySQL
│   └── db-init.sql      # سكريبت إنشاء قاعدة البيانات والجداول
├── controllers/
│   ├── authController.js      # منطق تسجيل الدخول والتسجيل
│   └── dashboardController.js # منطق Dashboard
├── middleware/
│   ├── auth.js          # Middleware للتحقق من JWT Token
│   └── validation.js    # Middleware للتحقق من صحة البيانات
├── routes/
│   ├── authRoutes.js    # مسارات API للمصادقة
│   └── dashboardRoutes.js # مسارات API للداشبورد
├── server.js            # ملف الخادم الرئيسي
├── package.json         # تبعيات Node.js
├── .env                 # متغيرات البيئة (يجب إنشاؤه)
└── README.md            # توثيق الباك إند
```

### API Endpoints

#### المصادقة (`/api/auth`)
- `POST /api/auth/register` - تسجيل مستخدم جديد
- `POST /api/auth/login` - تسجيل الدخول
- `GET /api/auth/me` - بيانات المستخدم الحالي (محمي)

#### الداشبورد (`/api/dashboard`)
- `GET /api/dashboard` - بيانات الداشبورد (محمي)
- `PUT /api/dashboard/profile` - تحديث الملف الشخصي (محمي)

### قاعدة البيانات

#### جدول users
- `id` - معرف فريد
- `name` - اسم المستخدم
- `email` - البريد الإلكتروني (فريد)
- `password` - كلمة المرور المشفرة
- `role` - الدور (user/admin)
- `created_at` - تاريخ الإنشاء
- `updated_at` - تاريخ التحديث

#### جدول products (جاهز للاستخدام المستقبلي)
- `id` - معرف المنتج
- `name` - اسم المنتج
- `description` - الوصف
- `price` - السعر
- `image_url` - رابط الصورة
- `category` - الفئة
- `stock` - المخزون

#### جدول orders (جاهز للاستخدام المستقبلي)
- `id` - معرف الطلب
- `user_id` - معرف المستخدم
- `total_amount` - المبلغ الإجمالي
- `status` - حالة الطلب
- `created_at` - تاريخ الإنشاء

## مجلد الفرونت إند (front/)

```
front/
├── components/          # مكونات React
│   ├── Footer.tsx
│   ├── Header.tsx
│   └── ProductCard.tsx
├── context/            # React Context
│   ├── AuthContext.tsx           # سياق المصادقة (localStorage)
│   ├── AuthContext.backend.tsx   # سياق المصادقة (API)
│   ├── CartContext.tsx
│   ├── LanguageContext.tsx
│   └── WishlistContext.tsx
├── data/               # بيانات وهمية
│   └── mockData.ts
├── pages/              # صفحات التطبيق
│   ├── Dashboard.tsx   # صفحة Dashboard (جديدة)
│   ├── Login.tsx
│   ├── Home.tsx
│   └── ...
├── utils/              # أدوات مساعدة
│   └── api.js          # دوال الاتصال بالباك إند
├── public/             # ملفات عامة
├── images/             # الصور
├── App.tsx             # المكون الرئيسي
├── index.tsx           # نقطة الدخول
├── index.html          # ملف HTML الرئيسي
├── package.json        # تبعيات المشروع
├── vite.config.ts      # إعدادات Vite
├── tsconfig.json       # إعدادات TypeScript
└── types.ts            # تعريفات TypeScript
```

## التحقق من النماذج (Validation)

### في الباك إند:
- استخدام `express-validator` للتحقق من البيانات
- التحقق من:
  - الاسم: 2-100 حرف
  - البريد الإلكتروني: صيغة صحيحة
  - كلمة المرور: 6 أحرف على الأقل، تحتوي على حرف كبير وصغير ورقم

### في الفرونت إند:
- التحقق الأساسي باستخدام HTML5 validation
- يمكن إضافة تحقق متقدم باستخدام مكتبات مثل `react-hook-form` أو `yup`

## الأمان

1. **تشفير كلمات المرور**: استخدام `bcryptjs`
2. **JWT Tokens**: للمصادقة والتفويض
3. **CORS**: مفعل للفرونت إند فقط
4. **Validation**: على مستوى الباك إند والفرونت إند
5. **SQL Injection Protection**: استخدام Prepared Statements

## الخطوات التالية

1. ✅ إنشاء هيكل المشروع
2. ✅ إعداد قاعدة البيانات
3. ✅ إنشاء API للمصادقة
4. ✅ إضافة التحقق من النماذج
5. ✅ إنشاء Dashboard
6. 🔄 ربط الفرونت إند بالباك إند (اختياري)
7. 🔄 إضافة المزيد من الميزات (المنتجات، الطلبات، إلخ)
